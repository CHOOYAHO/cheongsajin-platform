# 이시넘 담당 GPT 메모

최종 갱신: 2026-09-16

이 문서는 「이 시기를 넘기기」 프로젝트 담당 GPT가 청·사·진 홈페이지 담당 GPT에게 전달하는 운영 맥락이다. 홈페이지 작업을 시작할 때와 사용자에게 완료를 보고하기 전에 읽는다. 사용자의 별도 지시 없이 이 문서를 삭제하거나 덮어쓰지 않는다.

## 프로젝트 경계

- 이 폴더에서는 청·사·진 전용 홈페이지의 코드, Firebase, GitHub, 배포와 관련된 업무만 처리한다.
- 전체 사업 일정, 학교·멘토 연락, 현장 물품, 출력물, Notion 포트폴리오 템플릿 제작 등 홈페이지 밖의 운영 업무는 「이시넘」 프로젝트에서 관리한다.
- 홈페이지 작업 결과가 현장 운영이나 일정에 영향을 주면 사용자에게 분명히 알린다.

## 현재 가장 가까운 운영 일정

2026년 9월 18일 금요일에 두 학교 프로그램이 연속으로 진행된다.

- 광시중학교 3회기: 09:45~11:25
- 예산고등학교 4회기: 13:20~15:20

학교별 현재 진행 상태:

- 광시중학교: 2회기까지 완료. 9월 18일에 3회기 진행.
- 예산고등학교: 3회기까지 완료. 9월 18일에 4회기 진행.

광시중학교 3회기는 예산고등학교에서 같은 내용으로 먼저 운영했고 전반적으로 만족스럽게 진행됐다. 다만 AI 가상면접은 예산고 학생에게는 효과적이었지만 중학생에게는 조금 어려울 수 있다는 멘토 의견이 있었다.

## 긴급 홈페이지 작업

9월 18일 광시중학교 운영 전에 중학생용 AI 가상면접을 학교별로 분리·조정해야 한다.

### 확인된 현재 구현

- 클라이언트는 `schoolName`을 `runAiInterviewStep` Firebase Callable Function에 이미 전달한다.
- 서버 프롬프트는 대상을 현재 `중학생 또는 고등학생`으로 함께 처리하며 실제 학교별 질문 분기는 하지 않는다.
- 기존 예산고 면접 흐름은 현장에서 검증됐으므로 불필요하게 바꾸지 않는다.
- 2026-09-16 이시넘에서 별도로 복제해 확인했을 당시 최신 원격 커밋은 `81837eb Balance interview questions and add mentor results`였다.
- 당시 `pnpm build`는 통과했고 `pnpm lint`는 오류 없이 기존 경고 4개만 있었다.

### 구현 원칙

- 별도 가짜 화면이나 복제 컴포넌트를 만들지 않는다.
- 기존 AI 면접 화면, Firebase 저장 구조, 멘토 결과 조회 구조를 그대로 사용한다.
- `schoolName`이 광시중학교일 때 서버 질문, 힌트, fallback 질문과 평가 기준을 중학생 수준으로 분기한다.
- 예산고 학생과 그 밖의 사용자는 현행 검증 흐름을 유지한다.
- 실제 광시중 학생 화면과 관리자의 광시중 학생 역할 화면은 같은 컴포넌트, 같은 데이터, 같은 기능을 사용해야 한다.

광시중용 면접 기준:

- 질문을 짧게 쓰고 한 번에 한 가지를 묻는다.
- 학교생활, 친구와의 협력, 일상적인 선택, 관심 활동, 앞으로 해보고 싶은 일처럼 중학생이 답할 수 있는 상황을 사용한다.
- 어려운 직무·조직 용어와 복잡한 전문 상황을 줄인다.
- 과거 경험을 요구하거나 경험이 없다는 답변을 반복해서 추궁하지 않는다.
- 힌트는 완성 답안을 제시하지 않고 `무엇을 생각할지 → 그 이유 → 직업과 연결` 순서로 안내한다.
- 중학생의 비교적 짧은 답변을 고등학생과 같은 길이 기준으로 과도하게 감점하지 않도록 평가 로직을 검토한다.
- 면접의 진지함과 `희망 직업에 실제 지원하는 채용면접`이라는 성격은 유지한다.

### 검증과 완료 보고

수정 후 다음을 확인한다.

- 실제 광시중 학생 화면
- 관리자 광시중 학생 역할 화면
- 예산고 기존 면접 회귀 여부
- AI 정상 응답과 fallback 응답
- 면접 기록의 `aiInterviewLogs` 저장
- 멘토의 학생 결과 조회
- PC와 모바일 화면
- `pnpm build`
- `pnpm lint`
- Git 변경 내용과 민감정보 포함 여부
- 커밋과 `main` 푸시
- Firebase Functions 배포가 필요한 경우 실제 배포
- GitHub Pages 자동 배포와 실제 홈페이지 확인

완료 보고에는 변경 내용, 실제 화면과 관리자 역할 화면 반영 여부, 모바일 확인, build/lint 결과, 커밋 해시, 푸시·Functions·Pages 배포 상태를 포함한다.

## 회기 명칭 주의

홈페이지의 공식 프로그램 구조는 다음과 같다.

- 3회기: 진로 역량 갖추기, 업무 브레인스토밍, AI 가상면접
- 4회기: 전문강사 연계 직업 탐색
- 5회기: Notion 미래 포트폴리오와 나만의 청사진

현장에서는 학교 방문 순서에 따라 9월 18일 오후 활동을 `예산고 4회기`라고 부른다. 현재 준비 중인 Notion 포트폴리오는 홈페이지 구조상 5회기 성격이지만, 이번 예산고의 네 번째 학교 방문 일정에서 진행된다. 이 차이를 혼동해 홈페이지의 회기 구조를 임의로 바꾸지 않는다.

## 현재 홈페이지 작업 범위에서 제외된 사항

- 예산고 학생에게 구글 계정 준비 안내
- 외부 Notion 포트폴리오 템플릿 제작
- 9월 18일 현장 준비물 및 이동 계획
- 청·사·진 외 다른 사업 업무

위 사항은 이시넘에서 관리한다. 다만 홈페이지 수정 때문에 추가 안내나 현장 조치가 필요해지면 사용자에게 바로 알려야 한다.
