# 청·사·진 프로젝트 작업 지침

이 저장소는 청소년 진로 멘토링 프로그램 **청·사·진(청소년의 사기진작 진로멘토링)** 웹 플랫폼이다.

## 새 작업 시작 시

1. 먼저 `docs/HANDOFF.md`를 읽고 현재 상태와 다음 작업을 파악한다.
2. 사용자와는 편한 한국어 말투로 대화한다.
3. 수정 후 `pnpm build`, `pnpm lint`를 실행한다.
4. 정상 확인 후 `main` 브랜치에 커밋·푸시하면 GitHub Pages가 자동 배포된다.
5. Firebase 설정값이나 계정 정보는 코드와 문서에 직접 기록하지 않는다.

## 핵심 원칙

- **관리자(마스터) 역할 미리보기 동일성은 최우선 요구사항이다.** 실제 학생·멘토·교사 화면과 관리자가 선택한 각 역할 화면은 UI, 표시 조건, 버튼, 세부 페이지, 모바일 배치까지 항상 동일해야 한다.
- 관리자 역할 화면을 별도 모형·간이 컴포넌트·읽기 전용 화면으로 만들지 않는다. 실제 역할 화면과 같은 컴포넌트 및 코드 경로를 재사용한다.
- 관리자 역할 화면에서도 저장·조회·수정·알림·실시간 동기화 등 모든 기능이 실제 역할 화면과 동일하게 정상 작동해야 한다. `미리보기이므로 전송 불가`, 가짜 데이터, 임시 데이터, 별도 테스트 컬렉션 등으로 대체하지 않는다.
- 학생·멘토·교사 기능을 추가하거나 수정할 때는 해당 실제 계정 화면과 관리자 역할 화면 양쪽에서 같은 기능이 보이고 작동하는지 반드시 확인한다. 한쪽만 수정된 상태로 완료 처리하지 않는다.
- 청소년 개인정보를 최소한으로 수집한다.
- 학생·교사·멘토·관리자 PIN은 Firebase Functions와 Firestore를 통해 실제 검증한다. PIN 원문과 Firebase 비밀값을 코드·문서에 기록하지 않는다.
- 사용자가 명시적으로 "백업해 둬", "백업 후 삭제", "백업 기록을 남겨"처럼 백업을 요청한 자료는 로컬 백업 파일을 만들고 Git에도 커밋·푸시한다. 단, PIN 원문, Firebase 설정값, API 키, 마스터 코드 같은 비밀값은 백업 파일과 Git에 기록하지 않는다.
- 사용자가 명시하지 않았지만 안전을 위해 임의로 만든 작업 백업은 Git에 올리지 않는다.
- Firestore 보안 규칙을 완화하거나 공개 쓰기를 허용하지 않는다.
- PC와 모바일 레이아웃을 함께 확인한다.
- 하단 협력기관 로고의 크기와 위치는 사용자가 세밀하게 조정한 상태이므로 불필요하게 변경하지 않는다.
